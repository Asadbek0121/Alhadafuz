/**
 * Yandex Maps API key — client-side (NEXT_PUBLIC_* convention).
 * Key public bo'lishi shart (brauzerda ishlaydi), lekin env orqali boshqariladi
 * va osongina almashtiriladi.
 */
export const YANDEX_MAPS_KEY = process.env.NEXT_PUBLIC_YANDEX_MAPS_KEY || '';
